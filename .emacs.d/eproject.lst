;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("timetracker" "/home/xeno/dev/django/timetracker")
	("eproject" "/home/xeno/.emacs.d/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 0)
	(left . 0)
	(width . 158)
	(height . 60)))

