;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list nil)

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 459)
        (left . 800)
        (width . 97)
        (height . 25)))

